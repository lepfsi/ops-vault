# @ops-vault/web

Frontend OpsVault — Vite + React + TypeScript + Tailwind v4.

## Dev

```bash
# depuis la racine du monorepo
pnpm --filter @ops-vault/web dev
```

Proxy `/api` → `http://localhost:8787` (apps/api).
