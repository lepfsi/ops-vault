{
  "name": "ops-vault",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=@ops-vault/api --filter=@ops-vault/web",
    "dev:api": "pnpm --filter @ops-vault/api dev",
    "dev:web": "pnpm --filter @ops-vault/web dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22"
  }
}
